@typeparam TItem where TItem : class
@inherits MultiEditor<TItem, string>

<InputText @bind-Value="Value" disabled="@(IsEditableInConflict ? false : IsInConflict)" placeholder="@(IsInConflict ? ConflictValue : "")" />
